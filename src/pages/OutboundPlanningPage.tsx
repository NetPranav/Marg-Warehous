import { Box, Typography } from '@mui/material';

export default function OutboundPlanningPage() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h5" sx={{ fontWeight: 800, mb: 2 }}>Outbound Planning</Typography>
      <Typography color="text.secondary">Optimize truck routes and load distribution for outbound forwarding (Under Construction).</Typography>
    </Box>
  );
}
