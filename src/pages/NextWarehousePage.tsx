import { Box, Typography } from '@mui/material';

export default function NextWarehousePage() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h5" sx={{ fontWeight: 800, mb: 2 }}>Next Warehouse Coordination</Typography>
      <Typography color="text.secondary">Coordinate arrival windows and capacities with downstream warehouse nodes (Under Construction).</Typography>
    </Box>
  );
}
